document.addEventListener("DOMContentLoaded" ,()=>{
    const faqContainer = document.querySelector('.container');

    faqContainer.addEventListener("click" ,(e) =>{
        const groupHeader = e.target.closest(".acc-header")
    })
});